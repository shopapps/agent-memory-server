## Shared memory

Before project work, use the `shared-memory` skill to search for earlier
decisions, fixes, rules, and handoff facts that may apply.

{{PROJECT_SCOPE}}

After project work, use the same skill to save only new, checked facts that
will help later. Leave `agent_id` empty for facts Codex and Claude should share.
Keep tasks and work status in Beads or the project task tracker. Follow the
skill's privacy rules.
